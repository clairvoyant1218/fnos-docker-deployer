# 安全说明

飞牛 Docker 部署器需要调用宿主机 Docker，因此 FPK 生命周期脚本、CGI 页面和后台任务以 root 运行。这是一项高权限能力。

## 默认保护

- 仅在 fnOS 登录态下通过 CGI 打开，桌面入口只对管理员显示。
- 所有写操作使用 CSRF 随机令牌。
- 项目名、镜像名、GitHub URL、分支、端口、卷和设备都会再次校验。
- GitHub 仅接受公开 `https://github.com/作者/仓库` 地址，不接受 URL 中的 Token。
- 两段式端口默认绑定 NAS 局域网 IP；绑定全部接口需要单独确认。
- API Key、项目 `.env`、任务日志和部署数据不进入源码仓库。
- 卸载 FPK 不删除项目目录、容器、镜像或数据卷。
- AI 只能生成文本草稿，不能直接调用部署任务。

## 使用者责任

Docker 镜像、Compose 文件、root 用户、宿主机目录挂载和 privileged 模式都可能让容器获得 NAS 的高权限。只部署可信来源，并在执行前审阅上游文档和 Compose 文件。

不要把 `/vol1/Docker` 整体同步到公开位置，其中可能存在数据库密码、API Key 或其他私有环境变量。

## 报告问题

请在 GitHub 仓库中提交不包含密码、Token、私人路径和个人文件内容的最小复现。安全问题请勿附带真实凭据。
